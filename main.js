p = new Vector3(1,2,3);
function init() {
    print("CALL FROM INSIDE INIT");
}

function update(dt) {
    // print("CALL FROM INSIDE UPDATE: ", dt);
}

function shutdown() {
    print("CALL FROM INSIDE SHUTDOWN");
}
